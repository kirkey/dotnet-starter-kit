@page "/todos"

<PageHeader Title="Todos" Header="Todos" SubHeader="Manage your Todos." />

<EntityTable @ref="_table" TEntity="GetTodoResponse" TId="DefaultIdType" TRequest="TodoViewModel" Context="@Context">
    <EditFormContent>
        @if (!Context.AddEditModal.IsCreate)
        {
            <MudItem xs="12" md="6">
                <MudTextField Value="Context.AddEditModal.RequestModel.Id" Underline=false ReadOnly Label="Product Id" />
            </MudItem>
        }
        <MudItem xs="12" md="6">
            <MudTextField Label="Name" For="@(() => Context.AddEditModal.RequestModel.Name)" @bind-Value="Context.AddEditModal.RequestModel.Name" />
        </MudItem>
        <MudItem xs="12" md="6">
            <MudTextField Label="Description" For="@(() => Context.AddEditModal.RequestModel.Description)" @bind-Value="Context.AddEditModal.RequestModel.Description" />
        </MudItem>
        <MudItem xs="12" md="6">
            <MudTextField Label="Notes" For="@(() => Context.AddEditModal.RequestModel.Notes)" @bind-Value="Context.AddEditModal.RequestModel.Notes" />
        </MudItem>
        <MudItem xs="12" md="12">
            <MudItem>
                <div>
                    @if(!Context.AddEditModal.IsCreate)
                    {
                        <MudButton HtmlTag="label" Variant="Variant.Filled" Color="Color.Info" Size="Size.Small" Target="_blank"
                            StartIcon="@Icons.Material.Filled.RemoveRedEye" Style="margin-left: 5px;" >
                            View
                        </MudButton>

                        <MudButton HtmlTag="label" Variant="Variant.Filled" Color="Color.Error" Size="Size.Small"
                                   StartIcon="@Icons.Material.Filled.Delete" Style="margin-left: 5px;">
                            Delete
                        </MudButton>
                    }
                </div>
            </MudItem>
        </MudItem>
    </EditFormContent>

</EntityTable>