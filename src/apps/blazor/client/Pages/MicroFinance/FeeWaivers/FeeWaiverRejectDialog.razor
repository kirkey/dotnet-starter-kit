@namespace FSH.Starter.Blazor.Client.Pages.MicroFinance.FeeWaivers
@using FSH.Starter.Blazor.Infrastructure.Api

@inject ISnackbar Snackbar

<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.Cancel" Class="mr-3" Color="Color.Error" />Reject Fee Waiver
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudText Typo="Typo.body1" Class="mb-4">
            Are you sure you want to reject this fee waiver? The fee charge will remain at its current value.
        </MudText>
        <MudTextField @bind-Value="_reason"
                      Label="Rejection Reason"
                      Variant="Variant.Filled"
                      Lines="3"
                      Required="true"
                      Placeholder="Enter the reason for rejecting this waiver..." />
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Default" OnClick="Cancel" Disabled="_processing">Cancel</MudButton>
        <MudButton Color="Color.Error" Variant="Variant.Filled" OnClick="Confirm" Disabled="_processing || string.IsNullOrWhiteSpace(_reason)">
            @if (_processing)
            {
                <MudProgressCircular Size="Size.Small" Indeterminate="true" Class="mr-2" />
            }
            Reject
        </MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter]
    private IMudDialogInstance MudDialog { get; set; } = null!;

    [Parameter]
    public DefaultIdType WaiverId { get; set; }

    private string? _reason;
    private bool _processing;

    private void Cancel() => MudDialog.Cancel();

    private async Task Confirm()
    {
        if (string.IsNullOrWhiteSpace(_reason))
        {
            Snackbar.Add("Please provide a rejection reason.", Severity.Warning);
            return;
        }

        _processing = true;
        try
        {
            var command = new RejectFeeWaiverCommand
            {
                Id = WaiverId,
                RejectedByUserId = Guid.NewGuid(), // TODO: Get from current user
                RejectorName = "Current User", // TODO: Get from current user
                Reason = _reason!
            };
            await Client.RejectFeeWaiverAsync("1", WaiverId, command).ConfigureAwait(false);
            Snackbar.Add("Fee waiver rejected.", Severity.Success);
            MudDialog.Close(DialogResult.Ok(true));
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error rejecting waiver: {ex.Message}", Severity.Error);
        }
        finally
        {
            _processing = false;
        }
    }
}
