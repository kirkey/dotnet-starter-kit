@namespace FSH.Starter.Blazor.Client.Pages.MicroFinance.FeeWaivers

@inject ISnackbar Snackbar

<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.CheckCircle" Class="mr-3" Color="Color.Success" />Approve Fee Waiver
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudText Typo="Typo.body1" Class="mb-4">
            Are you sure you want to approve this fee waiver? This will waive the specified amount from the fee charge.
        </MudText>
        <MudTextField @bind-Value="_notes"
                      Label="Approval Notes (Optional)"
                      Variant="Variant.Filled"
                      Lines="2"
                      Placeholder="Enter any notes for this approval..." />
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Default" OnClick="Cancel" Disabled="_processing">Cancel</MudButton>
        <MudButton Color="Color.Success" Variant="Variant.Filled" OnClick="Confirm" Disabled="_processing">
            @if (_processing)
            {
                <MudProgressCircular Size="Size.Small" Indeterminate="true" Class="mr-2" />
            }
            Approve
        </MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter]
    private IMudDialogInstance MudDialog { get; set; } = null!;

    [Parameter]
    public DefaultIdType WaiverId { get; set; }

    private string? _notes;
    private bool _processing;

    private void Cancel() => MudDialog.Cancel();

    private async Task Confirm()
    {
        _processing = true;
        try
        {
            var command = new ApproveFeeWaiverCommand
            {
                Id = WaiverId,
                ApprovedByUserId = DefaultIdType.NewGuid(), // TODO: Get from current user
                ApproverName = "Current User" // TODO: Get from current user
            };
            await Client.ApproveFeeWaiverAsync("1", WaiverId, command).ConfigureAwait(false);
            Snackbar.Add("Fee waiver approved successfully.", Severity.Success);
            MudDialog.Close(DialogResult.Ok(true));
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error approving waiver: {ex.Message}", Severity.Error);
        }
        finally
        {
            _processing = false;
        }
    }
}
